
import { ISpinner } from "./Spinner";

const data: ISpinner = {
    color: "",
    position: "relative",
    size: "small"
};

const medium: ISpinner = {
    color: "",
    position: "absolute",
    size: "medium"
};

const large: ISpinner = {
    color: "",
    position: "absolute",
    size: "large"
};

export const mockSpinnerProps = {
    data,
    medium,
    large
};
import DefaultQuery from "../shared/default.gql";

const ViewInformationCards = `
    ${DefaultQuery}
    name
    backgroundColor
    showButton
    alignTitle
`;

export default ViewInformationCards;